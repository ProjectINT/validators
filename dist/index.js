const {
  getPlaceDetails
} = require('./getPlaceDetails');
const {
  placesTypes
} = require('./placesTypes');
const {
  placeIdFromText
} = require('./placeIdFromText');
module.exports = {
  getPlaceDetails,
  placesTypes,
  placeIdFromText
};